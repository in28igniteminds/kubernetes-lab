CREATE TABLE bank_interest_rate (
                         id   INTEGER      NOT NULL,
                         bank_name VARCHAR(128) NOT NULL,
                         interest_rate NUMERIC NOT NULL,
                         PRIMARY KEY (id)
);
